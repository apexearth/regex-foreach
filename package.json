{
  "name": "regex-foreach",
  "version": "1.0.0",
  "description": "Iterate through matches in a string.",
  "main": "regex.js",
  "scripts": {
    "test": "mocha regex-foreach.spec.js"
  },
  "keywords": [
    "regex",
    "foreach",
    "iterator",
    "each",
    "iterate"
  ],
  "author": {
    "name": "Christopher Jacobs",
    "email": "apexearth@gmail.com"
  },
  "repository": "https://github.com/apexearth/regex-foreach",
  "license": "ISC",
  "devDependencies": {
    "chai": "^3.5.0",
    "mocha": "^3.1.2"
  }
}
